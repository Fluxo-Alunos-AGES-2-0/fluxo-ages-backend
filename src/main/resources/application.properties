# Conexão JDBC - O formato é ${VARIAVEL:VALOR_PADRAO}
spring.datasource.url=jdbc:postgresql://localhost:${DB_PORT:5432}/${DB_NAME:fluxo_database}
spring.datasource.username=${DB_USER:fluxo_admin}
spring.datasource.password=${DB_PASSWORD:super_mega_fluxo_2ponto0_2026}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações do Hibernate (Code-First)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
server.port=8081